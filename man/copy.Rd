% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/z_tools.R
\name{copy}
\alias{copy}
\title{Copy a data frame to the clipboard for Excel}
\usage{
copy(
  x,
  file = "clipboard",
  row.names = FALSE,
  col.names = TRUE,
  sep = "\\t",
  dec = NULL,
  na = "",
  ...
)
}
\arguments{
\item{x}{A data frame, matrix, or object coercible to a tabular structure.}

\item{file}{Destination file. Defaults to \code{"clipboard"}.}

\item{row.names}{Logical. Whether to include row names. Default is \code{FALSE}.}

\item{col.names}{Logical. Whether to include column names. Default is \code{TRUE}.}

\item{sep}{Field separator. Defaults to tab (\code{"\\\\t"}), which is required for
reliable Excel clipboard pasting.}

\item{dec}{Decimal separator. If \code{NULL} (default), it is determined in the
following order:
\enumerate{
\item \code{getOption("ztools.dec")}, if set
\item Windows system locale (registry setting, if available)
\item \code{Sys.localeconv()$decimal_point} as fallback
}}

\item{na}{Character string used for missing values. Default is \code{""}.}

\item{...}{Additional arguments passed to \code{utils::write.table()}.}
}
\value{
Invisibly returns \code{NULL}. The function is called for its side effect
(copying data to clipboard).
}
\description{
Copy a data frame or matrix to the system clipboard in a format that is
optimized for pasting into Microsoft Excel.
}
\details{
This function is designed as an \strong{Excel clipboard bridge}, not a general
export utility. It formats tabular data in a way that Excel can directly
interpret when pasted, including locale-aware decimal handling on Windows.

The function is designed specifically for Excel interoperability via the
clipboard. Unlike generic export functions, it prioritizes compatibility
with Excel's parsing rules rather than portable file formats.

On Windows systems, the decimal separator is inferred from system locale
settings when not explicitly provided. This ensures pasted numeric values
are correctly interpreted by Excel in most regional configurations.
}
\section{Configuration}{

The default decimal separator can be overridden globally using:

\preformatted{
options(ztools.dec = ".")
}
}

\section{Behavior}{

\itemize{
\item Uses tab-separated values for maximum Excel compatibility
\item Attempts to match Excel's expected decimal separator automatically
\item Falls back safely to system locale or \code{"."} if detection fails
}
}

\examples{
\dontrun{
df <- data.frame(a = 1:3, b = c(1.1, 2.2, 3.3))

# Copy to clipboard for Excel
copy(df)

# Force decimal separator
copy(df, dec = ".")

# Set global preference
options(ztools.dec = ",")
copy(df)
}

}
